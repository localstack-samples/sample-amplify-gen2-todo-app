# Loaded by `vite --mode localstack` (see the `dev:localstack` script).
# Points the Amplify client at LocalStack's Cognito; the AppSync URL already
# comes from amplify_outputs.json.
VITE_LOCALSTACK_ENDPOINT=http://localhost.localstack.cloud:4566
